import React from "react";
import ReactDOM from "react-dom";

import UrlShortenerForm from "url-shortener-form";

const root = document.getElementById("root");

ReactDOM.render(<UrlShortenerForm />, root);
